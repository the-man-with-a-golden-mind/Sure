// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Js_dict = require("rescript/lib/js/js_dict.js");
var Js_json = require("rescript/lib/js/js_json.js");

var empty_theorems = [];

var empty = {
  entry: "",
  theorems: empty_theorems,
  emit: "none"
};

function requestOf(raw) {
  if (raw === "") {
    return empty;
  }
  try {
    var json = JSON.parse(raw);
    var dict = Js_json.classify(json);
    if (typeof dict !== "object") {
      return empty;
    }
    if (dict.TAG !== "JSONObject") {
      return empty;
    }
    var v = Js_dict.get(dict._0, "entry");
    var tmp;
    if (v !== undefined) {
      var s = Js_json.classify(v);
      tmp = typeof s !== "object" || s.TAG !== "JSONString" ? "" : s._0;
    } else {
      tmp = "";
    }
    return {
            entry: tmp,
            theorems: [],
            emit: "none"
          };
  }
  catch (exn){
    return empty;
  }
}

function namesOf(req) {
  if (req.entry === "") {
    return req.theorems;
  } else {
    return [req.entry].concat(req.theorems);
  }
}

exports.empty = empty;
exports.requestOf = requestOf;
exports.namesOf = namesOf;
/* No side effect */
